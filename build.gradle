apply plugin: 'java'

dependencies {
	implementation fileTree( dir: "../libs", include: ['JUtils.jar', 'TerminalWindow.jar'] )
}

ext {
	includeLibs = true
	copyLibs = true
	deployedProjDir = "deployed/Tools"
	deployedLibDir = "./"
	libDest = "deployed/Tools/libs"
	jarPathPrefix = "./libs/"
	
	mainClass = "app.MpegCombiner"
}

apply from: '../gradle/utilapp.gradle'